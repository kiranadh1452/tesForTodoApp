const {Given, When, Then, And, But } = require('@cucumber/cucumber');

Given('the user has browsed to home page', function () {
});

When('the user enters {string} in todo section', function (string) {
});

Then('the user should see {string} in todo list', function (string) {
});
